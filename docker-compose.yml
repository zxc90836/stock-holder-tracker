services:
  stockbot:
    image: stockbot
    container_name: stockbot
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DB_PATH: /data/stockbot.db
    volumes:
      - ./data:/data